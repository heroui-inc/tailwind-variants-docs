---
title: "Releases"
description: "Notable Tailwind Variants releases and changes, including performance, merge, and API updates."
---

## v3.4.0

Minor release adding multi-extend composition and an experimental debug mode, with faster invocations and more consistent returns ([#306](https://github.com/heroui-inc/tailwind-variants/pull/306)).

### Highlights

- **Multi-extend** — `extend` accepts a non-empty array of parents, merged left to right ([Extending](/docs/extending#multiple-parents))
- **Debug mode (experimental)** — `debug: true` logs how each invocation resolves; development and browser only ([Debugging](/docs/debugging))
- **Consistent returns** — `tv()`, `cn()`, `cnMerge()`, and `cx()` return `""` instead of `undefined` for empty class lists
- **Base-slot targeting** — array classes in `compoundVariants` and array variant values in slotted recipes now apply to the `base` slot, matching string behavior
- **Composition fixes** — plain and slotted recipes compose cleanly: string classes fold into the `base` slot instead of producing `[object Object]`
- **Whitespace-robust merging** — conflicts resolve across newlines and tabs, so multi-line template literals merge correctly
- **Performance** — roughly 1.30–1.37× faster core invocation paths compared to v3.3.1

### Multi-extend

Compose several parent recipes in one definition. Parents merge at definition time, so invocation speed is unaffected:

```ts
const button = tv({
  extend: [baseButton, focusable, animated],
  base: 'inline-flex items-center'
});
```

### Breaking changes & compatibility

- **Empty string instead of `undefined`** — `tv()`, `cn()`, `cnMerge()`, and `cx()` now return `""` when no classes remain, and the `CnReturn` type is now `string`. Update code that compares against `undefined` (`result === undefined`, optional chaining) to use falsy checks. Common patterns like `className={cn(...) || fallback}` keep working.
- **Stricter type checking** — unknown or misspelled keys in `variants`, `compoundVariants`, and `defaultVariants`, and unknown slot names in slot-shaped class values, now fail at compile time instead of being silently ignored at runtime. Fix any typos these errors surface; runtime output is unchanged for correct code.
- **Array classes target the base slot** — in slotted recipes, array-based `compoundVariants` classes and array variant values previously did not reach the base slot; they now apply to it. Audit slotted recipes that rely on the old behavior.

See [Migration](/docs/migration#to-v340) for upgrade steps.

## v3.3.1

Patch release fixing slots shared-state contamination ([#305](https://github.com/heroui-inc/tailwind-variants/issues/305)).

## v3.3.0

Stronger TypeScript inference, built-in conflict resolution, and less setup than earlier v3 releases.

### Highlights

- **Fully rewritten in TypeScript** — stronger inference for variants, slots, and `extend`
- **Conflict resolution included** — no `tailwind-merge` install required for TV
- **Performance improvements** — faster recipe resolution and class merging
- **Optional `tailwindcss` peer** — use TV outside Tailwind-only setups more easily

### Conflict resolution

The default `tailwind-variants` entry includes merge logic inspired by [tailwind-merge](https://github.com/dcastil/tailwind-merge). Install one package:

```bash
npm install tailwind-variants
```

- Existing `twMergeConfig` objects generally keep working — prefer `{ extend, override }`
- Lite (`tailwind-variants/lite`) still has no merge
- Utilities: `tv`, `createTV`, `cn`, `cnMerge`, `cx`

### Behavior changes

- **`slots: {}`** — explicit empty object enables slot mode with implicit `base`; omit `slots` for string return
- **Extended metadata types** — describe merged variants, slots, and keys at runtime
- **`cn(...)(config)`** — use `cnMerge` instead (see [Migration](/docs/migration))

### Upgrade steps

1. Remove `tailwind-merge` if it was only installed for TV
2. Keep `twMergeConfig` as-is; use `extend` / `override` for custom tokens
3. Switch to `/lite` only when you intentionally skip merge

```diff
- npm i tailwind-variants tailwind-merge
+ npm i tailwind-variants
```

### Acknowledgements

Conflict resolution draws on MIT-licensed work from [tailwind-merge](https://github.com/dcastil/tailwind-merge), [clsx](https://github.com/lukeed/clsx), and [cnfast](https://github.com/aidenybai/cnfast). See [Acknowledgements](/docs/acknowledgements).

---

## Earlier v3 releases

### v3.2.2

- `cn` returns a string directly; `cnMerge` added for custom config
- Better TypeScript support without Proxy types

### v3.2.0

- `cx` replaces `cnBase` for lightweight concat
- `cn` defaults to conflict resolution on the default build

### v3.0.0

- Default vs lite build split
- Major performance improvements
- Framework-agnostic API retained

See [Migration](/docs/migration) for step-by-step upgrade paths.
