---
title: "Variants"
description: "Define base styles and variant keys like color, size, and disabled on a tv recipe for typed call-site props."
---

import {
  VariantsBadgeDemo,
  VariantsBooleanDemo,
  VariantsColorDemo,
  VariantsMultiDemo
} from '@/components/examples';
import {
  variantsBadge,
  variantsBoolean,
  variantsColor,
  variantsMulti
} from '@/demos/code';

Variants turn one component definition into many visual states. You set a **base** — shared classes every call site gets — then add keys like `variant`, `size`, or `disabled`.

## Base + variants

<DemoPreview code={variantsColor} lang="ts">
  <VariantsColorDemo />
</DemoPreview>

Base classes apply first. Variant classes layer on top. Conflicting utilities resolve automatically in the default build.

## Multiple variants

Combine as many variant keys as you need. Each key is an independent axis (`color` × `size` × `disabled`):

<DemoPreview code={variantsMulti} lang="ts">
  <VariantsMultiDemo />
</DemoPreview>

## Boolean variants

Use `true` / `false` keys for state flags:

<DemoPreview code={variantsBoolean} lang="ts">
  <VariantsBooleanDemo />
</DemoPreview>

Boolean variants work well for `disabled`, `active`, `loading`, or feature toggles.

## Array values

Variant values accept arrays. TV flattens them like `clsx`:

<DemoPreview code={variantsBadge} lang="ts">
  <VariantsBadgeDemo />
</DemoPreview>
